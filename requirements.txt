# NDIS Opportunity Explorer - Python dependencies
# Pinned to recent, stable releases. The app is local-first and needs nothing
# beyond these to run end-to-end against the bundled sample data.

streamlit==1.40.2
pandas==2.2.3
numpy==2.1.3
plotly==5.24.1

# Testing (optional for running the app, required for development)
pytest==8.3.4

# Optional: enables LLM-written narrative insights when an API key is present.
# The app degrades gracefully to a built-in rule-based generator if this is
# not installed or no key is configured, so it is safe to omit.
openai==1.57.4
